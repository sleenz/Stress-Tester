# CLAUDE.md

> Last verified against the codebase: 2026-07-14, Bahana Stress Tester Phase 7a+7b (correlation network companion view, both sub-targets done) + post-7b refinements (edge hover, 7b MST→complete graph, Turbo gradient palette + colorbar, solid-only edges) + data source consolidation (LSEG Data Library added as primary price source, AlphaVantage/TwelveData/FMP deleted, yfinance kept as the sole fallback) + macro data source consolidation (FRED and Trading Economics both deleted outright from `macro_data.py`; LSEG Data Library is now primary for all 9 macro variables, yfinance the fallback for 5 of them, 4 with no fallback at all by explicit confirmed decision — see Load-bearing decisions below) + a Regime + DCC-GARCH correlation diagnostics panel added to the Sector Shock tab (regime timeline w/ correlation overlay, regime-conditioned correlation small multiples, identity-fallback surfacing — diagnostic only, no core-algorithm changes) + a second, separate HMM emission cluster separability diagnostic (Step 2d) added alongside it (covariance ellipses over the real standardised feature space, Mahalanobis boundary-point flagging with soft-posterior hover, degenerate/coincident-ellipse area table — also diagnostic only; required additive-only new fields on `RegimeResult` — `feature_matrix`/`hmm_means`/`hmm_covars` — since those weren't previously exposed, see Load-bearing decisions below).
> This file is not self-maintaining. If you change a module described below, re-verify the relevant line before trusting it — a stale entry here is worse than no entry.
> The phased build/production-readiness plan this fork is executed against lives at [`docs/BUILD_SPEC.md`](docs/BUILD_SPEC.md) — read it before starting any phase's work.

## What this is

**Bahana Stress Tester** is a scoped fork of PortfolioOptimizer for Bahana TCW: a Streamlit web app + Python library covering **Portfolio Input + Stress Testing + Risk Analytics** — multi-source price ingestion, historical crisis replay (actual per-stock returns with beta-scaled proxy fallback), DCC-GARCH/Student-t-copula/HMM-regime sector shock propagation, Leontief macro contagion, Monte Carlo simulation, and a baseline VaR/Sharpe/GARCH risk dashboard. It has meaningful Indonesian-market (IDX, `.JK` tickers) support alongside US equities. Built for a solo/small-team quant workflow, not a regulated production trading system — treat it as a calculation-assistance tool (see README.md's disclaimer).

Optimization, Portfolio Builder (ranking/correlation-network), Factor Analysis, Reports, and Stock Valuation are **out of scope** and have been removed from this fork — see `docs/BUILD_SPEC.md` Phase 0/1 for the dependency audit that confirmed what could be deleted cleanly. Risk Analytics was originally a documented Phase 6 fast-follow with its backing modules kept dormant; Phase 6 has since restored `3_Risk_Analytics.py` against them (see below) — they are active, not dormant, as of this version.

## Directory map

| Path | Purpose |
|---|---|
| `app/Home.py`, `app/pages/1_Portfolio_Input.py`, `app/pages/2_Stress_Testing.py`, `app/pages/3_Risk_Analytics.py` | The entire retained Streamlit UI. Page 1 also owns Presets as a tab. Page 2 has five tabs: Historical Scenarios, Monte Carlo, Sector Shock, Macro Contagion, and Correlation Network. The Correlation Network tab has two independent sections: (7a) a ticker-level MST colored by real per-ticker P&L from a selected Historical or Sector Shock scenario — Macro Contagion is deliberately not offered as a P&L source, see below — and (7b) a **complete graph** (every sector pair, not an MST — reverted from an initial MST-based design per user feedback) of calm-vs-crisis regime correlation fed by the fitted Sector Shock engine's DCC-GARCH/HMM regime models, requiring that fit to have run first. Both sections render edges as solid lines with a full Turbo gradient (chosen over a diverging red-white-blue scale, which washes out to near-white at correlation ≈ 0) plus a visible colorbar, and an invisible per-edge midpoint marker so hovering anywhere along an edge — not just near its endpoints — shows its exact correlation value; see the Load-bearing decisions entry on `_render_correlation_network()` below. The Sector Shock tab also has a "Regime ↔ DCC-GARCH Correlation Diagnostics" expander (diagnostic only, no effect on stress P&L): a regime timeline with a mean-off-diagonal DCC correlation line overlaid, regime-conditioned correlation small multiples (one heatmap per HMM state, `RdBu_r` to match every other correlation heatmap in this file — not `RdYlGn`/`heat_color.py`, which was deleted in Phase 1 and was a ranking colormap, not a correlation one), and identity-fallback graying-out for any regime with `n_observations < 5`; see `_render_regime_dcc_diagnostics()` and its module-level helper functions, defined before `st.set_page_config()` so they're available to tab3 despite `_edge_color_gradient()`/`_render_correlation_network()` being defined later in the script for tab5. A second, separate expander, "HMM Emission Cluster Separability (State-Quality Check)", sits alongside it (sibling, not merged in) — `_render_emission_separability_diagnostic()`: a scatter of the real standardised `(rolling_vol, mean_return)` feature points colored by HMM hard label, with real fitted-covariance ellipses (1σ/2σ, via eigendecomposition — genuinely elliptical, not circles) overlaid per state, Mahalanobis-distance boundary-point flagging (black outline) with the real soft posterior on hover for every point, and a `sqrt(det(Σ))` area-proxy table so degenerate/near-coincident states are visible as numbers too — not time-ordered, and deliberately answers a different question than the DCC-overlay panel above it (state separability in feature space, not regime/DCC alignment over time). Both panels are diagnostic-only; neither affects the stress P&L below them. Page 3 (restored in Phase 6) covers VaR/CVaR, drawdowns, correlations, volatility (incl. EWMA), tail risk, risk contribution/hedge classification, and Effective Number of Bets via PCA. |
| `src/data/` | Price fetch with `LSEGSource` (LSEG Data Library) as primary and `YFinanceSource` as the sole fallback — AlphaVantage/TwelveData/FMP were deleted outright, not just deprioritized (see Load-bearing decisions below). Pickle cache, sector classification (TRBC/GICS, incl. IDX overrides in `lseg_sectors.py`), LSEG-primary/yfinance-fallback macro data (`macro_data.py` — both FRED and Trading Economics deleted outright, see Load-bearing decisions below). |
| `src/risk/` | `DCCGARCHModel` (sector-level), `StudentTCopula`, `MarketRegimeDetector` (HMM), `LeontifContagionEngine`, sector/stock beta, macro sensitivity — all used by Stress Testing. `metrics.py` (`RiskMetrics`), `var.py` (`VaRCalculator`), `garch.py` (`GARCHModel`, `ewma_volatility`) were kept dormant through Phase 1-5 and are now **active** — `app/pages/3_Risk_Analytics.py` imports all three (Phase 6, done). |
| `src/simulation/` | `MonteCarloSimulator` (4 methods), legacy uniform-shock `StressTester` (`scenarios.py`), real-per-stock `HistoricalStressor` (`historical_scenarios.py`), `SectorStressEngine`, `MacroStressEngine` (Leontief). |
| `src/portfolio/` | `HoldingsTracker` only. `calculator.py`/`rebalancer.py` were deleted in the fork trim (Optimization/Monitoring out of scope) — `src/portfolio/__init__.py` was edited accordingly; don't re-add imports of those two names without restoring the files. |
| `src/portfolio_builder/` | `network.py` is now **active** — `app/pages/2_Stress_Testing.py`'s Correlation Network tab imports `compute_distance_matrix`, `build_ticker_mst`, `filter_edges_by_threshold`, `correlation_from_distance`, `node_color_for_percentile`, `NetworkConfig`, `CorrelationNetworkConfig`, `NetworkStyleConfig` — the low-level ticker-MST and node-coloring functions only. `build_sector_mst` and `edge_color_for_correlation` are imported by network.py but **no longer used by the page** — 7b builds its complete graph directly with `networkx` instead of `build_sector_mst` (see above), and edge coloring is now `2_Stress_Testing.py`'s own page-local `_edge_color_gradient()` (a full Turbo colorscale, chosen over a diverging red-white-blue scale to avoid washing out near correlation ≈ 0, plus a colorbar legend), not `network.py`'s 2-color `edge_color_for_correlation()`; both were deliberately left unchanged in `network.py` rather than modified/removed, since that module's own functions are still "reviewed and merged" surface a future consumer might use. It computes ticker-level correlation via `returns.corr()` directly with pandas (7a) and sector-level correlation via `MarketRegimeDetector.get_regime_correlation()` (7b), and never calls `build_semantic_zoom_network()`/`build_correlation_matrix()`/`build_sector_distance_matrix()` (the functions that accept a `correlation=`/cache argument, or that aggregate ticker-level distances into sector-level) at all, so `UniverseCache`/`cache.py` are bypassed by not being invoked, not by being fed a substitute value. `cache.py` itself is consequently still dormant (kept only because `network.py` has an unconditional top-level import of `UniverseCache` from it, as a type annotation). `fetch.py`, `ranking.py`, `metrics.py`, `heat_color.py`, `ff5_overlay.py` remain deleted — do not re-import them. |
| `src/utils/` | `preset_manager.py` (portfolio preset persistence, no network dependency), `settings_manager.py`, logging, helpers. |
| `docs/architecture.md` | Full module-by-module detail for the ORIGINAL (pre-fork) PortfolioOptimizer codebase — still useful for understanding *why* a retained module (e.g. DCC-GARCH, sector beta) is built the way it is, but describes several modules (Optimization, Portfolio Builder ranking, Factor Analysis, Reports, Valuation) that no longer exist in this fork. Read it for retained-module context only; don't use it to relocate a deleted feature. |
| `docs/BUILD_SPEC.md` | This fork's phase-by-phase build/production-readiness plan — the authoritative source for what's done, what's dormant-on-purpose, and what's still pending. |

## Load-bearing decisions

- **`src/risk/sector_beta.py` is load-bearing from two different call sites** — `SectorStressEngine.fit()` (`sector_stress.py`) uses `SectorBetaAnalyzer.compute()` for the cross-sector beta matrix shown in the UI, while the actual per-stock betas used for P&L come from `compute_all_stock_betas()` in `stock_sector_beta.py`. Separately, `MacroStressEngine.fit()` (`macro_stress.py`) lazily imports `SectorBetaAnalyzer` again for sector-return construction (Step 3 of its fit) — a real, core-path usage, not a fallback. Neither `sector_beta.py` nor `stock_sector_beta.py` can be merged or deleted. `sector_beta.py`'s own docstring on `compute_stock_betas_vs_portfolio_sectors()` used to claim it was the primary path `SectorStressEngine.fit()` uses — that claim was wrong (the method is called nowhere outside its own definition) and has been corrected (`docs/BUILD_SPEC.md` Phase 3 item 1, done).
- **Two package `__init__.py` landmines exist in this codebase's history — watch for a third.** Deleting a module that a sibling module's package `__init__.py` still imports breaks *every* consumer of that package, not just the deleted module's own callers (Python runs a package's `__init__.py` on any submodule import). This is exactly how `src/portfolio/__init__.py` would have broken Portfolio Input if `calculator.py`/`rebalancer.py` had been deleted without also trimming `__init__.py`'s imports (now fixed — `__init__.py` only exports `holdings.py` names). `src/risk/__init__.py` imports from `metrics.py`/`var.py`/`garch.py`, but since those three files were kept in place throughout (dormant through Phase 1-5, active as of Phase 6 — never deleted), that package's `__init__.py` never needed a change either way. Before deleting any module in a package with siblings, check the package's `__init__.py` first.
- **Preset loading populates `current_holdings` via a live price fetch, not just `weights`/`portfolio_value`** — `src/utils/preset_manager.py::apply_preset_to_state()` only ever sets `tickers`/`weights`/`portfolio_value`/`settings`/`loaded_preset_id`; it deliberately still does not touch `current_holdings`. `app/pages/1_Portfolio_Input.py`'s `_load_preset_and_populate_holdings()` wraps it and additionally derives `shares = weight * portfolio_value / current_price` — kept out of `preset_manager.py` on purpose to keep that module free of a network/`DataManager` dependency.
- **Portfolio Presets is a tab on Portfolio Input, not a separate page** — merged in because preset-loading and holdings-editing are the same workflow.
- **Correlation Network's node coloring reuses `network.py::node_color_for_percentile()` unmodified, but feeds it per-ticker P&L percentile instead of the Portfolio Builder composite-ranking percentile it was originally written for** — same tri-tier function, different input dimension entirely; this was an explicit design instruction (Phase 7a), not a coincidental reuse.
- **Correlation Network's P&L source is deliberately limited to Historical and Sector Shock scenario results — Macro Contagion is never offered.** `MacroStressEngine.run_stress()` (`macro_stress.py`) computes `direct`/`total` return per-ticker by looking up `contagion.h_initial`/`contagion.leontief_total` keyed on **sector only** (`macro_stress.py`, the `for ticker, weight in weights.items()` loop) — every ticker sharing a sector gets an identical return, scaled only by that ticker's own weight for the dollar P&L. That's real position-sized P&L, but not real per-ticker *differentiation* — coloring nodes by it would visually imply a per-ticker signal that doesn't exist. Don't add Macro Contagion as a Correlation Network P&L source without first adding a real ticker-level allocation step (e.g. ticker's sector-beta × its sector's distress, the same shape Sector Shock already uses).
- **`MarketRegimeDetector.get_regime_correlation()` returns only a DataFrame — no flag distinguishing a real averaged matrix from its sub-5-observation identity-matrix fallback.** The Correlation Network tab's regime overlay (Phase 7b) needs to warn the user before that fallback renders as if it were a real network, but modifying that function is against this project's absolute constraints. Instead, `2_Stress_Testing.py`'s `_n_common_regime_obs()` read-only replicates the function's own aligned-observation count (same date-intersection between `regime_result.state_sequence` and `dcc_result.conditional_volatilities.index`) to decide whether to show the warning, then still calls the real function afterward to get the actual matrix. Verified bit-exact against the real function on both a normal-history case and a manufactured 3-observation case — if you touch either side of this duplication, re-verify they still agree.
- **Both Correlation Network sections (7a ticker, 7b sector-regime) render through one shared `_render_correlation_network()` helper in `2_Stress_Testing.py` — edge hover and edge color both work differently than they first appear.** (1) Edge hover: a Plotly `mode="lines"` trace only matches hover near its actual plotted points (the two edge endpoints), not along the interior of the segment — hovering the middle of a long edge shows nothing unless something else is plotted there. The fix is an additional, separate `go.Scatter` trace of invisible (`opacity=0`) markers, one per edge, positioned at each edge's exact midpoint and carrying the `ρ=` hover text — this is why the function builds `edge_traces` (visible lines, `hoverinfo="skip"`) and a single combined `edge_hover_trace` (invisible markers, `hoverinfo="text"`) separately, rather than putting hover text on the lines themselves. That same invisible-marker trace also carries the visible colorbar legend (`marker.colorscale`/`cmin=-1`/`cmax=1`/`colorbar=...`), since a colorbar has to be attached to a trace with a real colorscale mapping, not to individually-colored line traces. (2) Edge color: `_edge_color_gradient()`, a page-local function using `plotly.colors.sample_colorscale("Turbo", ...)`, not `network.py`'s `edge_color_for_correlation()` — Turbo instead of a 2-flat-color or red-white-blue diverging scale, because a diverging scale washes out to near-white around correlation ≈ 0, making weak correlations hard to distinguish; Turbo stays vivid and discriminable across the whole `[-1, 1]` range. `network.py`'s own function was left untouched (still importable, just unused by this page) rather than modified, consistent with not rewriting a reviewed/merged module's function as a side effect of a page-level styling change. (3) All edges are solid lines — no dashing anywhere, per explicit user request; MST/backbone-vs-additional-threshold-edge is still conveyed by line **width** (thick vs thin), just not by dash style. (4) 7b passes `mst_edges=set(graph.edges())` (i.e., every edge) into this shared function rather than a real MST subset, since it now renders a complete graph — every edge gets the "is_mst" thick-width styling and none get the thin one, which is intentional (there's no more MST-vs-extra distinction to convey once every pair is drawn).
- **`RegimeResult` (`regime_detection.py`) gained three new, defaulted (`= None`) fields — `feature_matrix`, `hmm_means`, `hmm_covars` — added for the emission cluster separability diagnostic (2d) and nothing else.** Before this, `MarketRegimeDetector.fit()` computed the standardised feature matrix `X` and the fitted `GaussianHMM.means_`/`covars_` internally but discarded them once `state_sequence`/`state_probabilities` were derived — no consumer needed them. They're now captured and relabelled with the exact same `state_order` permutation `fit()` already computes for `state_sequence`/`state_probabilities`/the transition matrix, so all four stay in consistent calm→crisis order. Populating them required touching `fit()`'s return statement and adding one new private static method (`_full_covariances()`), but no line of the actual HMM-fitting or relabelling algorithm changed — this is data capture, not a rewrite, consistent with this project's absolute constraint against modifying reviewed/merged core algorithm modules.
- **The installed `hmmlearn==0.3.3`'s public `GaussianHMM.covars_` property is bugged for `covariance_type="spherical"`** — it returns shape `(n_components * n_features, n_features, n_features)` instead of `(n_components, n_features, n_features)`, because `hmmlearn.utils.fill_covars()` calls `np.ravel()` on the internal `_covars_` array (which redundantly repeats each state's scalar variance across the feature axis for `"spherical"`) without accounting for that redundancy. `MarketRegimeDetector._full_covariances()` bypasses the public property for `"spherical"` only, reconstructing the correct shape directly from `model._covars_`; `"full"`/`"diag"`/`"tied"` read from the public property unchanged (verified correct for those three). `2_Stress_Testing.py` always fits with the default `covariance_type="full"` (only `n_states`/`n_init` are exposed in its "Model Configuration" UI), so this bug was never reachable through the app — the workaround exists because the `RegimeConfig.covariance_type` field itself supports `"spherical"`, and a diagnostic panel shouldn't silently mis-render ellipses if that ever becomes UI-configurable. Covered by a dedicated `covariance_type="spherical"` case in `regime_detection.py`'s own smoke test — re-verify against whatever hmmlearn version is actually installed if this is ever touched, since it's a real upstream-library bug, not a version-invariant guarantee.
- **The emission cluster separability panel (2d) and the Regime ↔ DCC-GARCH panel (2a-2c) are two different diagnostics answering two different questions, deliberately not merged into one expander.** 2a-2c is time-ordered and checks whether regime labels coincide with real DCC correlation spikes over the date axis. 2d is not time-ordered at all — it's a snapshot of the fitted Gaussian emissions in 2D standardised-feature space, checking whether `n_states` is separable/well-specified independent of when each observation occurred. Rendering both through the same function or expander would visually imply they're the same check; they render through separate functions (`_render_regime_dcc_diagnostics()` vs `_render_emission_separability_diagnostic()`) in separate, sibling expanders instead. Neither panel acts on what it observes — 2d explicitly does not change `n_states` as a side effect of a degenerate or near-coincident ellipse it renders; that's flagged as an observation for the reviewer, not auto-corrected.

- **`src/data/sources.py::LSEGSource` is the primary price source; `AlphaVantageSource`/`TwelveDataSource`/`FMPSource` were deleted, not deprecated-in-place** — `DataManager.__init__()` (`data_manager.py:54-58`) now only ever constructs `[LSEGSource(), YFinanceSource()]`. `LSEGSource.fetch_prices()` requests a single field (`fields=["TRDPRC_1"]`) from `lseg.data.get_history()` deliberately — the library only builds a `(ticker, field)` column MultiIndex when *multiple* fields are requested (confirmed by reading `lseg/data/_access_layer/_history_df_builder.py` directly), so a single-field request returns a flat, ticker-named-column DataFrame, matching every other source's shape without extra reshaping code. Session credentials come from `LSEG_APP_KEY` (env var) or the library's own `lseg-data.config.json` discovery — same two-path pattern `lseg_sectors.py::LSEGSectorFetcher` already uses, but this is a **separate LSEG integration**, not a shared one: one calls `ld.get_data()` for TRBC reference data, the other calls `ld.get_history()` for price time series. **Verified end-to-end in a sandbox with the real `lseg-data` package installed but no credentials/session available**: `open_session()` does not raise when no local Workspace/Eikon proxy is reachable — it silently returns a non-connected session, and the failure only surfaces later as a plain exception from the first `get_history()` call. `_ensure_session()`'s try/except therefore does not catch this particular failure mode (`mark_unavailable()` is never called), but the outer `fetch_prices()`/`DataManager.get_price_data()` exception handling still catches it correctly on every call and falls through to `YFinanceSource` — confirmed via a live run showing `Attempting to fetch from LSEG` → graceful failure → `Attempting to fetch from YFinance` in order. This means an unconfigured LSEG session is retried on every single `get_price_data()` call rather than being marked permanently unavailable after the first failure — functionally fine (fallback still fires every time), just a minor repeated-attempt overhead worth knowing about if fetch latency ever becomes a concern.
- **`src/data/macro_data.py` now sources all 9 macro variables from the LSEG Data Library as primary, with yfinance as a per-variable fallback where a real equivalent ticker exists — Trading Economics and FRED were both deleted outright, not deprioritized.** This was a two-step migration (see `docs/BUILD_SPEC.md` for the full history): FRED was removed first (replaced with an LSEG fallback for 5 of 9 variables), then Trading Economics was removed too (LSEG promoted from fallback to primary for those 5, and newly added as primary for the 4 remaining variables that had been Trading-Economics-only). `_fetch_te_market()`/`_fetch_te_indicator()`/`_fetch_fred()` and `MacroDataConfig.te_api_key`/`fred_api_key` are all gone; `tradingeconomics`/`fredapi`/`pandas_datareader` are gone from `requirements.txt`; `TE_API_KEY`/`FRED_API_KEY` are gone from `.env.example`. `_fetch_lseg()` (`macro_data.py`) is a third, independent LSEG integration — distinct from `LSEGSource` (`sources.py`) and `LSEGSectorFetcher` (`lseg_sectors.py`) — mirroring `LSEGSource.fetch_prices()`'s single-field `get_history(fields=["TRDPRC_1"])` pattern, but managing its own lazy session via `_ensure_lseg_session()` so it doesn't silently depend on call order with the other two. Verified end-to-end in the same credential-less sandbox as `LSEGSource`: an unconfigured LSEG session fails gracefully, the variable lands in `missing_variables`, and `fetch()` never raises.
- **Only 5 of 9 macro variables have a real yfinance fallback; the other 4 (CPO, NICKEL, BI_RATE, CHINA_PMI) have none at all, by explicit, confirmed user decision.** yfinance has no equivalent for Bursa Malaysia CPO futures, LME base metals, a foreign central bank's policy rate, or a PMI series — there is nothing to fall back to. DXY/VIX/IDR_USD/COAL/US_10Y do have real yfinance tickers (`.DXY`/`.VIX`/`IDR=`/`MTFc1`/`US10YT=RR` primary → `DX-Y.NYB`/`^VIX`/`IDR=X`/`MTF=F`/`^TNX` fallback) and were migrated with a genuine safety net. **All 9 primary LSEG RICs, and especially the 2 economic-indicator RICs (`IDCBIR=ECI` for BI_RATE, `CNPMI=ECI` for CHINA_PMI), are unverified** — no live LSEG credentials were available in this sandbox to confirm any of them against a real session. Market-instrument RICs (indices, FX, rates, exchange-traded commodities) follow well-documented, stable Refinitiv conventions; economic-indicator RICs are catalog-specific per country/series and meaningfully less certain. If any of these 9 codes are wrong, that variable silently lands in `missing_variables` (confirmed non-crashing, see `docs/BUILD_SPEC.md`) — for the 4 with no fallback, that's a full loss of the variable, a known and explicitly accepted tradeoff, not an oversight. Confirm all 9 RICs against a live session before trusting this pipeline in production — same spirit as the `TLKM.JK` sector-override caveat in `lseg_sectors.py`.

## Known placeholders — do not mistake these for real, load-bearing values

- **`src/data/lseg_sectors.py::IDX_SECTOR_OVERRIDES["TLKM.JK"]`**: applied unconditionally in code, but carries an inline `# Confirm vs LSEG assignment` comment — i.e. shipped before the override was actually confirmed correct. Given IDX names are core to this product, this is `docs/BUILD_SPEC.md` Phase 2 item 3 — confirm or correct before relying on it.
- **`src/risk/sector_beta.py::compute_stock_betas_vs_portfolio_sectors()` is NOT on `SectorStressEngine.fit()`'s path** — grep-verified: it's called nowhere in the codebase outside its own definition. Its docstring used to claim otherwise (fixed, `docs/BUILD_SPEC.md` Phase 3 item 1); the real per-stock-beta path is `compute_all_stock_betas()` in `src/risk/stock_sector_beta.py`.
- **`scenarios.py`'s legacy uniform-shock dict is `UNIFORM_SHOCK_SCENARIOS`; `historical_scenarios.py`'s actual-per-stock-returns list is `PER_STOCK_CRISIS_SCENARIOS`** — previously both were named `HISTORICAL_SCENARIOS` (a naming collision fixed in `docs/BUILD_SPEC.md` Phase 3 item 2). Both are still live and used for different things — don't conflate them.
- **`sector_stress.py` reporting `converged=False` for a fit `dcc_garch.py` logged as `converged=True`** — a known, not-yet-fixed bookkeeping bug. `docs/BUILD_SPEC.md` Phase 2 item 1 — deferred, not yet addressed in this fork.
- **`3_Risk_Analytics.py`'s EWMA Volatility panel used to plot the wrong series** — it computed `ewma_volatility(returns, decay=0.94)` but then plotted `(returns*weights).sum(axis=1).rolling(20).std()` instead of that computed series (a pre-fork, pre-existing bug). Fixed on restoration (Phase 6): the panel now computes EWMA vol directly on the portfolio return series and plots that.
- **`src/portfolio_builder/network.py`'s module docstring used to justify staying at ticker-level plain `.corr()` (instead of extending `DCCGARCHModel` to ticker level) by citing "an unresolved convergence-misreport issue"** attributed to the since-deleted `fetch.py`. That claim never traced to anything in `dcc_garch.py`'s actual code — what's really there is a hard `ConvergenceError` guard plus an explicit constant-vol fallback (see `docs/architecture.md`'s `src/risk/` section). Fixed on activation (Phase 7a): the docstring now describes the real mechanism and no longer cites the unverified reason. The underlying decision (plain `.corr()` at ticker level) was never wrong — only the stated justification was.

## Absolute constraints

- Streamlit + the existing Python backend only. No React, Redis, graph database, WebSockets, or 3D rendering anywhere in this codebase (confirmed: no `package.json`, no such dependencies in `requirements.txt`, no matching imports).
- Do not modify the existing DCC-GARCH or sector-normalization modules' core algorithms as a side effect of unrelated work — extend via new config fields/functions, not by rewriting what's already reviewed and merged.
- Do not re-import from a deleted module (`src/optimization/`, `src/factors/`, `src/reports/`, `src/valuation/`, `src/portfolio/{calculator,rebalancer}.py`, `src/portfolio_builder/{fetch,ranking,metrics,heat_color,ff5_overlay}.py`) or a deleted page (`2_Optimization.py`, `5_Monitoring.py`, `6_Factor_Analysis.py`, `7_Reports.py`, `8_Stock_Valuation.py`, `10_Portfolio_Builder.py`) — these are gone, not just unlinked from nav. (`3_Risk_Analytics.py` was restored in Phase 6 — it is no longer on this deleted list.)
- Python 3.11 or 3.12 only (pinned in `runtime.txt`) — required for `hmmlearn` wheel availability on Streamlit Community Cloud; the Sector Shock stress test's HMM regime-conditioning does not run on other versions.

## Where to go next

For full module-by-module detail on the modules this fork retained — dataclass listings, exact function signatures, and the reasoning behind each non-obvious choice (e.g. why HRP-vs-ticker-level-DCC-GARCH tradeoffs were made the way they were) — see [`docs/architecture.md`](docs/architecture.md), keeping in mind it also documents several now-deleted modules. For what's done, pending, or intentionally dormant in this fork, [`docs/BUILD_SPEC.md`](docs/BUILD_SPEC.md) is authoritative. Read the relevant section before touching a module; this file is an index, not a substitute for reading the code you're changing.
