# Data & Computation
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Financial Data
# LSEG Data Library is the primary price source (DataManager tries it first);
# yfinance is the only fallback (used automatically if no LSEG session/app key
# is configured, or if the fetch fails) — also still used directly for TRBC
# sector lookups' fallback path in lseg_sectors.py.
lseg-data>=2.0.0
yfinance>=0.2.40
requests>=2.31.0

# Risk & Volatility (DCC-GARCH, Student-t copula, HMM regime detection)
arch>=6.3.0
hmmlearn>=0.3.3

# Machine Learning (macro sensitivity regression, contagion network, stock-sector beta OLS)
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Visualization
plotly>=5.18.0
matplotlib>=3.7.0  # not imported directly anywhere in this scope, but pandas' Styler.background_gradient()
                    # (used 3x in 2_Stress_Testing.py) requires it at call time — found by the Phase 1
                    # smoke test (ImportError only surfaces when that code path actually runs), not by
                    # static import grep, since it's pandas' own internal dependency, not ours.

# Web Interface
streamlit>=1.31.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.3
loguru>=0.7.0
joblib>=1.3.0
tqdm>=4.66.0
networkx>=3.0
tradingeconomics>=0.3.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
